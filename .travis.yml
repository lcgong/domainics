language: python
python:
  - "3.6"
  - "3.7"

services:
  - postgresql

ddons:
  postgresql: "9.6"

install:
    - pip install .

script: py.test -s

before_script:
  - psql -h localhost -U postgres -c "CREATE DATABASE test WITH ENCODING='UTF8'"
